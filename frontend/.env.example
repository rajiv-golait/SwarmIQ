# SwarmIQ FastAPI (dev): proxy target for Vite
VITE_API_PROXY_TARGET=http://127.0.0.1:8000

# Optional: if set, browser calls this host directly (CORS must allow the frontend origin)
# VITE_API_BASE_URL=http://127.0.0.1:8000
