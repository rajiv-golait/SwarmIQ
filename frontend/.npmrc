# Keep npm cache under your local profile (helps when the repo is on R: or a sync folder).
# If install still fails with TAR_ENTRY_ERROR, run in PowerShell before `npm install`:
#   $env:TEMP = "$env:LOCALAPPDATA\Temp"; $env:TMP = $env:TEMP
cache=${LOCALAPPDATA}/npm-cache-swarmiq-frontend
